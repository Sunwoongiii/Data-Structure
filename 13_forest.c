/*
Forest
- A forest is a set of n>=0 disjoint trees
- We can combine the trees of a forest into a single BT
  1. if T1, T2, ... Tn is a forest of trees, then the BT corresponding to this forest, denoted by B(T1, ... Tn)
  2. is empty if n = 0
  3. has root equal to root(T1); has left
*/